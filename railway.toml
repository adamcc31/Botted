[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "bash slingger/start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[volumes]]
mountPath = "/app/logs"

[environments.production]
startCommand = "bash slingger/start.sh"
